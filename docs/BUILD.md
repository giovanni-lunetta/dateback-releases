# Build Notes: `memory-organizer` Binary

This document captures what is known in this repository about how the `memory-organizer` binary is used and where the build command might live.

## What Is Confirmed In This Repo

- App runtime expects prebuilt binaries at:
  - `assets/bin/memory-organizer`
  - `assets/bin/ffmpeg`
- Electron packaging includes `assets/bin/**/*` as extra resources:
  - see `package.json` `build.extraResources`
- No `npm` script in this repo runs PyInstaller directly.
- No `.github/workflows` directory exists in this repo.

## Packaging Output (App Release Artifacts)

- App packaging in this repo is driven by `electron-builder`:
  - `npm run build`
  - `npm run build:mac`
- Release artifacts (for example macOS DMG) are generated by `electron-builder` during those build commands.

## Polar Environment Modes (Production vs QA/Sandbox)

License validation now supports an environment switch with an explicit sandbox guard:

- `DATEBACK_POLAR_ENV` controls requested Polar API mode.
- `DATEBACK_ALLOW_SANDBOX=1` is required to actually enable sandbox mode.
  - Default behavior (or missing allow flag): `https://api.polar.sh`
  - Sandbox enabled only when both are set:
    - `DATEBACK_POLAR_ENV=sandbox`
    - `DATEBACK_ALLOW_SANDBOX=1`
  - Sandbox host: `https://sandbox.polar.sh`
- Organization ID resolution:
  - Production: `POLAR_ORG_ID` (fallback to built-in production org id)
  - Sandbox: `POLAR_ORG_ID_SANDBOX` (fallback to `POLAR_ORG_ID`, then built-in production org id)

Build examples:

- Production/default:
  - `npm run build`
- QA/Sandbox:
  - `DATEBACK_POLAR_ENV=sandbox DATEBACK_ALLOW_SANDBOX=1 POLAR_ORG_ID_SANDBOX=<sandbox-org-id> npm run build`

Recommended for QA builds (so QA can coexist with prod on one machine):

- Use a distinct app name and bundle ID for QA, for example:
  - `extraMetadata.productName=DateBack QA`
  - `extraMetadata.build.appId=com.giovannilunetta.dateback.qa`
- Keep signing/notarization/release automation unchanged for production unless intentionally creating a separate QA distribution flow.

## Spec Files Present

- Root: `memory-organizer.spec` (canonical)

## What We Searched

Search terms used:

- `pyinstaller`
- `memory-organizer.spec`
- `--specpath`
- `--onefile`
- `--name`

Results found only:

- The two spec files themselves.
- Historical release-note mentions of PyInstaller.
- No executable build command using either spec.

## Canonical Build Command

Run from the repo root:

```bash
pip3 install -r python/requirements.txt
pyinstaller memory-organizer.spec
```

Output: `dist/memory-organizer`

After verifying the binary works, copy it to `assets/bin/`:

```bash
cp dist/memory-organizer assets/bin/memory-organizer
```

The root `memory-organizer.spec` is the canonical spec. The `python/memory-organizer.spec` file has been removed.
